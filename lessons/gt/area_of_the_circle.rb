def area(radius)
   puts "the area of the circle is #{3.14*radius*radius}"
end

puts 'enter the radius'
radius = STDIN.gets.chomp.to_f
area(radius)

puts 'enter the radius'
radius = STDIN.gets.chomp.to_f
area(radius)